mongodb+srv://raulrosado91:<password>@buyshare.t3ffzxr.mongodb.net/test

router.post('/login',
    passport.authenticate('local', {session: false}),
    async (req,res,next) =>{
        try {
            const userInfo = req.user
            delete userInfo['password']
            console.log(userInfo)
            res.json({
                status:true,
                user:userInfo
            });    
        } catch (error) {
            next(error);
        }
    }
);